{
  "name": "gothic-baroque-feature-mapper",
  "private": true,
  "scripts": {
    "dev:backend": "backend\\.venv\\Scripts\\python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload --app-dir backend",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start"
  }
}
